// AGP 8.7.3 / Kotlin 2.1.0 — sdks/android 툴체인과 같은 조합(그쪽 주석 참조: AGP 9는 쓸 수 없다).
plugins {
    id("com.android.library") version "8.7.3" apply false
    kotlin("android") version "2.1.0" apply false
}
