node_modules/
*.log
.DS_Store
dist/

# 툴 생성 아티팩트 (oh-my-claudecode 세션/상태)
.omc/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Gradle / Kotlin
.gradle/
build/
bin/

# Dart / Flutter
.dart_tool/
pubspec.lock

# 백엔드 런타임 산출물
.rynl10n-storage/
*.db
*.db-journal

# 내부 문서(SoT=Craft 문서로 이전, 로컬에만 유지)
# OPERATIONS.md는 제외 — README·backend·SDK 문서가 링크로 가리키는 대고객 문서라 추적한다.
HANDOVER.md
CLAUDE.md

# Android 로컬 SDK 경로(환경마다 다름 — ANDROID_HOME으로도 해결됨)
local.properties
