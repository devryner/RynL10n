node_modules/
*.log
.DS_Store
dist/

# 툴 생성 아티팩트 (oh-my-claudecode 세션/상태)
.omc/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Gradle / Kotlin
.gradle/
build/
bin/

# Dart / Flutter
.dart_tool/
pubspec.lock

# 백엔드 런타임 산출물
.rynl10n-storage/
*.db
*.db-journal

# 내부 문서(SoT=Craft 문서로 이전)
# OPERATIONS.md(대고객 운영 가이드)와 HANDOVER.md(저장소 지도)는 추적한다 —
# 추적 파일들이 링크로 가리키므로 클론에 없으면 GitHub에서 404가 된다.
# CLAUDE.md는 AGENTS.md의 사본이라 로컬에만 둔다.
CLAUDE.md

# Android 로컬 SDK 경로(환경마다 다름 — ANDROID_HOME으로도 해결됨)
local.properties

# Web SDK 게시 산출물(prepack에서 생성) — 저장소 개발은 .ts를 그대로 실행한다
sdks/web/dist/
